# Recovery A — Recomendação de apply

## Executar `apply` agora?

**Não.** A Recovery A ficou limitada a **dry-run local**, sem Supabase, sem readiness oficial e sem `--apply`.

## Quando reavaliar

- **BASA** e **DoD:** candidatos a **dry-run loader oficial** sobre o conjunto canónico quando a equipa quiser promover artefactos (após revisão humana dos `incompleto`).
- **ESA:** só após existir pipeline público com itens standardized > 0.

## Próximos comandos (sem apply)

Ver `recovery_a_apply_recommendation.json` para a lista curta sugerida.
