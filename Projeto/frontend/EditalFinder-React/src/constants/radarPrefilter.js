/** Pré-filtro anti-ruído do catálogo antes do Worker (Fase 0.9A). */
export const RADAR_USE_CATALOG_PREFILTER = true;

/** Incrementar ao mudar regras de exclusão (invalida coerência com caches antigos). */
export const RADAR_PREFILTER_VERSION = '9a-v1';
